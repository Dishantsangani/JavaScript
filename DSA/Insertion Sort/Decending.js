let decending = [64, 34, 25, 57, 86, 97, 27, 12];
function dev(data) {
  for (let i = 0; i < array.length; i++) {
    let current = data[i];
    let prev = i - 1;
    while (prev>=0  && ) {
        
    }
  }
}
dev(decending);
console.log("decending: ", decending);
