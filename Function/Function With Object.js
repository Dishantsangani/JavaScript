const user = {
  name: "Dishant",
  price: 199,
};
function handleobject(uer) {
  console.log(`Username Is ${uer.name}And Price Is ${uer.price}`);
}
handleobject(user);

// Funcion with Array
// const myNew = [200, 400, 600, 500];
// function retval(arr) {
//   return arr[1];
// }
// console.log(retval(myNew));

// Ex
let dev = [1, 2, 3, 4, 5, 6];
function greet(arr) {
  return arr[4];
}
console.log(greet(dev));
